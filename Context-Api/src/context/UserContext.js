import { createContext } from "react";

const UserContext = createContext();  //Here i have create a globally context which thing there things nested and i want pas a data. So i have create a that thing globally context.

export default UserContext;